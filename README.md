# Board AI — internal design preview

Password-protected prototype. The page HTML is AES-256-GCM encrypted (PBKDF2-SHA256, 310k iterations); the copy and markup are only readable with the access password.

Note: files under `assets/` are **not** encrypted and remain directly fetchable without the password.

Built from a separate source repo — do not edit these files by hand.
